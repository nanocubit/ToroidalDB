name: ToroidalDB Full Verification

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        components: rustfmt, clippy

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

    - name: Check formatting
      run: cargo fmt --all -- --check

    - name: Run clippy
      run: cargo clippy -- -D warnings

    - name: Run tests
      run: cargo test --verbose

    - name: Build release
      run: cargo build --release --verbose

    - name: Run extended functionality tests
      run: cargo test extended_functionality_tests -- --nocapture

    - name: Run distributed execution tests
      run: cargo test distributed_execution_tests -- --nocapture

    - name: Run performance optimization tests
      run: cargo test performance_optimization_tests -- --nocapture

    - name: Run toroidal topology tests
      run: cargo test toroidal_topology_tests -- --nocapture

    - name: Run comprehensive hybrid tests
      run: cargo test comprehensive_hybrid_tests -- --nocapture

    - name: Run MCP/CLI tests
      run: cargo test mcp_cli_tests -- --nocapture

    - name: Run all integration tests
      run: cargo test integration_tests -- --nocapture

  security-scan:
    runs-on: ubuntu-latest
    needs: verify

    steps:
    - uses: actions/checkout@v4

    - name: Run cargo-deny
      uses: EmbarkStudios/cargo-deny-action@v1
      with:
        command: check

    - name: Run audit
      run: cargo audit

  build-docker:
    runs-on: ubuntu-latest
    needs: [verify, security-scan]
    if: github.ref == 'refs/heads/main'

    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/toroidal-db:latest,${{ secrets.DOCKER_USERNAME }}/toroidal-db:${{ github.sha }}

  deploy-staging:
    runs-on: ubuntu-latest
    needs: build-docker
    if: github.ref == 'refs/heads/main'
    environment: staging

    steps:
    - name: Deploy to staging
      run: |
        echo "Deploying to staging environment..."
        # Add deployment commands here

  deploy-production:
    runs-on: ubuntu-latest
    needs: [build-docker, deploy-staging]
    if: github.ref == 'refs/heads/main' && contains(github.event.head_commit.message, '[deploy]')
    environment: production

    steps:
    - name: Deploy to production
      run: |
        echo "Deploying to production environment..."
        # Add deployment commands here